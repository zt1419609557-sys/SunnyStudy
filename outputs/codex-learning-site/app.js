const storageKey = "codex-learning-progress";
const lessons = [
  { id: "browser-world", type: "文章", title: "浏览器、URL、DNS：一次访问网站到底发生了什么？", track: "基础认知", level: "小白", duration: "12 分钟", summary: "把“输入网址”拆成浏览器、DNS、服务器和渲染四步，先建立世界观。", metaphor: "像你在餐厅点餐：你说菜名，服务员去后厨，最后把热乎乎的菜端回来。", learned: ["知道 URL、域名、IP 的关系", "知道 DNS 的作用", "知道浏览器不只是显示页面，还会执行脚本和渲染布局"], steps: ["输入网址，浏览器解析 URL", "DNS 把域名翻译成 IP", "浏览器建立连接并发起请求", "服务器返回 HTML、CSS、JS", "浏览器渲染页面并执行脚本"], exercise: "打开浏览器开发者工具的 Network 面板，刷新一个页面，找到第一个 document 请求。", snippet: "URL -> DNS -> IP -> HTTPS -> HTML/CSS/JS -> 页面", tags: ["浏览器", "DNS", "HTTP", "渲染"] },
  { id: "frontend-basics", type: "课程", title: "HTML / CSS / JavaScript：先写出一个能看、能点、能动的页面", track: "前端基础", level: "小白", duration: "30 分钟", summary: "先学页面骨架，再学美化，最后让按钮真的动起来。", metaphor: "HTML 是房屋框架，CSS 是装修，JavaScript 是通电后的开关和机关。", learned: ["会写一个有标题、按钮、卡片的页面", "知道 CSS 负责样式和布局", "知道 JavaScript 负责交互和状态"], steps: ["先写 HTML 结构", "再用 CSS 做布局与配色", "最后用 JavaScript 处理点击和数据"], exercise: "改一个按钮颜色，再让它在点击后显示一句欢迎语。", snippet: 'document.querySelector("button").addEventListener("click", function () { alert("你好，Codex 学院！"); });', tags: ["HTML", "CSS", "JavaScript", "前端"] },
  { id: "http-api", type: "文章", title: "HTTP / API / JSON：前端是怎么把数据要回来的？", track: "前后端连接", level: "入门", duration: "18 分钟", summary: "理解请求、响应、状态码、JSON 和 fetch，学会让页面和服务端对话。", metaphor: "API 像点单窗口，前端负责下单，后端负责做菜，JSON 是打包好的菜单袋。", learned: ["知道请求和响应是什么", "知道状态码为什么重要", "会用 fetch 拉取接口数据"], steps: ["浏览器发出请求", "服务器返回 JSON", "前端把 JSON 转成页面"], exercise: "用 fetch 请求一个公开接口，打印返回 JSON。", snippet: 'const data = await fetch("/api/posts").then(function (r) { return r.json(); });', tags: ["HTTP", "API", "JSON", "Fetch"] },
  { id: "database-sql", type: "课程", title: "数据库入门：表、索引、SQL 和连接方式", track: "数据库", level: "入门", duration: "25 分钟", summary: "学会把数据放进表里，搞懂主键、索引、SQL 以及前后端如何连到数据库。", metaphor: "数据库像仓库，表像货架，索引像货架上的标签，查询就是找货。", learned: ["会设计最小表结构", "知道主键和索引", "会写 SELECT、INSERT、UPDATE"], steps: ["建表", "插入数据", "查询与筛选", "加索引提高查询速度"], exercise: "为“文章列表”设计一张表，并写出查询语句。", snippet: "SELECT id, title FROM articles WHERE status = 'published' ORDER BY created_at DESC;", tags: ["数据库", "SQL", "索引", "MySQL"] },
  { id: "fullstack-glue", type: "课程", title: "前后端连起来：做一个最小可用的留言板", track: "实战", level: "进阶", duration: "30 分钟", summary: "把前端表单、后端接口和数据库真正连成一条线。", metaphor: "前端是顾客，后端是柜台，数据库是仓库，接口是传送带。", learned: ["会设计表单 -> API -> 数据库的流转", "知道错误处理和返回值", "知道环境变量为什么要放在后端"], steps: ["前端收集表单", "后端校验并写库", "返回成功或失败结果"], exercise: "设计一个留言板的最小闭环。", snippet: 'app.post("/api/messages", async function (req, res) { /* validate -> insert -> return */ });', tags: ["前后端", "CRUD", "接口", "表单"] },
  { id: "deploy-domain", type: "课程", title: "部署上线与购买域名：把网站送到公网", track: "部署发布", level: "进阶", duration: "28 分钟", summary: "学会把网站上线，并把域名指向它，完成从本地到公网的最后一步。", metaphor: "域名像门牌号，DNS 像电话簿，服务器像真正的房子。", learned: ["理解域名、DNS、HTTPS", "知道静态托管和服务器部署的区别", "会看上线前检查清单"], steps: ["准备或购买域名", "配置 DNS 记录", "部署应用并绑定域名", "申请 HTTPS 并测试", "上线后观察日志和监控"], exercise: "为你的网站写一份上线检查表。", snippet: "A 记录 / CNAME / HTTPS / 环境变量 / 回滚", tags: ["域名", "DNS", "HTTPS", "部署"] },
  { id: "stability-security", type: "文章", title: "稳定性与安全：缓存、备份、权限和常见坑", track: "稳定性", level: "进阶", duration: "16 分钟", summary: "知道什么该缓存、什么不能暴露、什么必须备份，少踩大坑。", metaphor: "给网站戴上安全带和保险，平时看不见，但真出事的时候特别重要。", learned: ["知道缓存的作用", "知道敏感信息不能写死在前端", "知道备份和监控为什么不能省"], steps: ["加缓存", "做权限控制", "做日志与监控", "准备回滚方案"], exercise: "找出项目里 3 个需要放进环境变量的配置。", snippet: "process.env.DATABASE_URL", tags: ["安全", "缓存", "监控", "备份"] },
  { id: "codex-coach", type: "工具", title: "让 Codex 当你的助教：提问、拆题、检查和优化", track: "Codex 使用", level: "小白", duration: "10 分钟", summary: "学会让 Codex 帮你解释概念、拆项目、检查风险和做学习计划。", metaphor: "Codex 不是魔法按钮，而是会陪你拆题的助教。", learned: ["会把背景和目标说清楚", "会让 AI 输出结构化结果", "会让 AI 做检查和优化"], steps: ["先说目标和背景", "再要求输出结构化结果", "最后让 Codex 做检查与优化"], exercise: "把你的项目目标发给 Codex，让它拆成 7 天计划。", snippet: "请用小白能懂的方式解释这个概念，并给我 1 个练习。", tags: ["Codex", "提示词", "学习方法", "AI"] }
];
const roadmap = [
  { title: "第一阶段：看懂网站是怎么跑的", description: "先搞懂浏览器、域名、DNS、服务器和数据库各自负责什么。", lessons: ["browser-world", "http-api"], checklist: ["看懂一次访问流程", "知道请求和响应", "能说清 DNS 的作用"] },
  { title: "第二阶段：自己写出页面", description: "先会 HTML，再会 CSS，最后让 JavaScript 负责交互。", lessons: ["frontend-basics"], checklist: ["写出页面骨架", "做出布局和样式", "让按钮会动"] },
  { title: "第三阶段：接上数据库和业务", description: "把表单、接口和数据库连起来，做一个真正有数据流转的网站。", lessons: ["database-sql", "fullstack-glue", "stability-security"], checklist: ["会写 SQL", "知道接口怎么设计", "懂得保护配置和数据"] },
  { title: "第四阶段：上线、发布、域名", description: "从本地到公网，把网站部署好、绑定域名并做好安全与回滚。", lessons: ["deploy-domain", "codex-coach"], checklist: ["能讲清部署步骤", "会配置域名", "会用 Codex 当学习搭子"] }
];
const promptCards = [
  { title: "把概念讲成人话", body: "请用小白能懂的方式解释【主题】，先讲生活中的比喻，再讲真实原理，最后给我一个最小例子。", tips: ["适合第一次接触新概念", "可以要求加图示说明"] },
  { title: "拆成 7 天学习计划", body: "我想学【目标】，请帮我拆成 7 天计划，每天只安排 1 小时，最后给我每天的练习题。", tips: ["适合做长期路线", "适合新手不知从哪开始"] },
  { title: "检查项目风险", body: "请检查这个网站从开发到上线可能遇到的 10 个坑，并按严重程度排序，附上规避建议。", tips: ["适合部署前排雷", "适合复盘现有项目"] },
  { title: "拆分前后端职责", body: "如果我要做一个【功能】，请分别给前端、后端、数据库列出需要做的事，并标出接口边界。", tips: ["适合做需求拆解", "适合团队协作"] }
];
const deploymentCards = [
  { title: "1. 购买或准备域名", intro: "先选一个容易记的名字，再去注册商购买。不要追求花哨，简单、好记、和业务相关最重要。", items: ["检查域名是否可用", "优先选择短、清晰、好拼写的名字", "确认续费价格和年限", "开启隐私保护（如果有）"] },
  { title: "2. 配置 DNS 指向网站", intro: "把域名的记录指向你的服务器或静态托管平台，让门牌号真正指向房子。", items: ["A 记录一般指向服务器 IP", "CNAME 常用来指向平台提供的地址", "等待 DNS 生效可能需要时间", "改记录前先备份当前配置"] },
  { title: "3. 发布与申请 HTTPS", intro: "把代码部署到线上，并让浏览器看到安全的小锁图标，避免用户输入时被截获。", items: ["配置环境变量", "跑一次发布前检查", "确认首页、登录、表单、接口都正常", "测试失败回滚流程"] },
  { title: "4. 上线后观察与迭代", intro: "上线不是终点。要看日志、看访问、看错误，慢慢把网站变得更稳定。", items: ["记录 404、500 和异常请求", "备份数据库和配置", "关注性能和加载速度", "准备下一版优化清单"] }
];
const typeFilters = ["全部", "文章", "课程", "工具"];
const progressStore = loadProgress();
let activeType = "全部";
let queryText = "";
let selectedLessonId = lessons[0].id;
const lessonCountEl = document.getElementById("lessonCount");
const completedCountEl = document.getElementById("completedCount");
const visibleCountEl = document.getElementById("visibleCount");
const progressPercentEl = document.getElementById("progressPercent");
const lessonListEl = document.getElementById("lessonList");
const lessonDetailEl = document.getElementById("lessonDetail");
const promptGridEl = document.getElementById("promptGrid");
const deploymentGridEl = document.getElementById("deploymentGrid");
const roadmapGridEl = document.getElementById("roadmapGrid");
const typeFiltersEl = document.getElementById("typeFilters");
const searchInputEl = document.getElementById("searchInput");
const resetProgressBtn = document.getElementById("resetProgress");
const toastEl = document.getElementById("toast");
lessonCountEl.textContent = String(lessons.length);
renderRoadmap();
renderFilters();
renderPrompts();
renderDeployment();
renderAll();
searchInputEl.addEventListener("input", function (event) { queryText = event.target.value.trim().toLowerCase(); renderAll(); });
resetProgressBtn.addEventListener("click", function () { if (!progressStore.completed.length) { showToast("当前还没有学习进度"); return; } progressStore.completed = []; saveProgress(); renderAll(); showToast("已清空学习进度"); });
lessonListEl.addEventListener("click", function (event) { const card = event.target.closest("[data-lesson-id]"); if (!card) return; const lessonId = card.getAttribute("data-lesson-id"); if (event.target.closest("[data-action='toggle']")) { toggleLesson(lessonId); return; } selectedLessonId = lessonId; renderAll(); document.getElementById("lessonDetail").scrollIntoView({ behavior: "smooth", block: "nearest" }); });
lessonListEl.addEventListener("keydown", function (event) { const card = event.target.closest("[data-lesson-id]"); if (!card) return; if (event.key === "Enter" || event.key === " ") { event.preventDefault(); selectedLessonId = card.getAttribute("data-lesson-id"); renderAll(); } });
typeFiltersEl.addEventListener("click", function (event) { const chip = event.target.closest("[data-type]"); if (!chip) return; activeType = chip.getAttribute("data-type"); renderAll(); });
promptGridEl.addEventListener("click", async function (event) { const button = event.target.closest("[data-copy]"); if (!button) return; const text = button.getAttribute("data-copy"); try { await navigator.clipboard.writeText(text); showToast("提示词已复制"); } catch (error) { showToast("复制失败，可手动选中后复制"); } });
lessonDetailEl.addEventListener("click", function (event) { const button = event.target.closest("[data-detail-action]"); if (!button) return; const action = button.getAttribute("data-detail-action"); if (action === "toggle") toggleLesson(selectedLessonId); if (action === "next") { const visible = filteredLessons(); const currentIndex = visible.findIndex(function (lesson) { return lesson.id === selectedLessonId; }); if (!visible.length) return; const nextLesson = visible[(currentIndex + 1) % visible.length]; selectedLessonId = nextLesson.id; renderAll(); } });
function renderAll() { const visible = filteredLessons(); visibleCountEl.textContent = String(visible.length); renderFilters(); renderLessons(visible); renderDetail(visible); updateProgress(); }
function renderRoadmap() { roadmapGridEl.innerHTML = roadmap.map(function (phase, index) { const related = phase.lessons.map(function (lessonId) { const lesson = lessons.find(function (item) { return item.id === lessonId; }); return lesson ? lesson.title : ""; }).filter(Boolean); return ['<article class="roadmap-card">', '<span class="roadmap-index">' + String(index + 1).padStart(2, "0") + "</span>", "<h3>" + escapeHtml(phase.title) + "</h3>", "<p>" + escapeHtml(phase.description) + "</p>", '<ul class="roadmap-list">' + phase.checklist.map(function (item) { return "<li>" + escapeHtml(item) + "</li>"; }).join("") + "</ul>", '<div class="lesson-meta" style="margin-top:14px;">' + related.map(function (title) { return '<span class="meta-tag">' + escapeHtml(title) + "</span>"; }).join("") + "</div>", "</article>"].join(""); }).join(""); }
function renderFilters() { typeFiltersEl.innerHTML = typeFilters.map(function (type) { const active = type === activeType ? " active" : ""; return '<button type="button" class="chip' + active + '" data-type="' + escapeHtml(type) + '">' + escapeHtml(type) + "</button>"; }).join(""); }
function renderLessons(list) { lessonListEl.innerHTML = list.map(function (lesson) { const completed = progressStore.completed.indexOf(lesson.id) !== -1; const selected = lesson.id === selectedLessonId ? " selected" : ""; const done = completed ? " completed" : ""; const badgeText = completed ? "已完成" : lesson.track; return ['<article class="lesson-card' + selected + done + '" data-lesson-id="' + lesson.id + '" tabindex="0" role="button">', '<div class="lesson-top">', '<div>', '<div class="lesson-meta">', '<span class="meta-tag">' + escapeHtml(lesson.type) + "</span>", '<span class="meta-tag">' + escapeHtml(lesson.level) + "</span>", '<span class="meta-tag">' + escapeHtml(lesson.duration) + "</span>", "</div>", "<h3>" + escapeHtml(lesson.title) + "</h3>", "</div>", '<span class="pill">' + escapeHtml(badgeText) + "</span>", "</div>", '<p class="lesson-summary">' + escapeHtml(lesson.summary) + "</p>", '<div class="lesson-footer">', '<div class="tags">' + lesson.tags.map(function (tag) { return '<span class="tiny-tag">' + escapeHtml(tag) + "</span>"; }).join("") + "</div>", '<button type="button" class="tiny-button" data-action="toggle">' + (completed ? "取消完成" : "标记已学") + "</button>", "</div>", "</article>"].join(""); }).join(""); }
function renderDetail(list) { const lesson = lessons.find(function (item) { return item.id === selectedLessonId; }) || list[0] || lessons[0]; if (!lesson) { lessonDetailEl.innerHTML = '<div class="detail-empty"><span class="empty-badge">没有匹配结果</span><h3>试试换个关键词或者筛选条件</h3><p>你可以先看“全部”，或者搜索你想学的词，比如“数据库”“域名”“部署”。</p></div>'; return; } selectedLessonId = lesson.id; const completed = progressStore.completed.indexOf(lesson.id) !== -1; lessonDetailEl.innerHTML = ['<div class="detail-head">', '<div class="detail-heading">', '<div>', '<span class="section-kicker">' + escapeHtml(lesson.track) + "</span>", '<h3>' + escapeHtml(lesson.title) + "</h3>", "</div>", '<span class="pill">' + (completed ? "已完成" : "学习中") + "</span>", "</div>", '<div class="detail-tags">' + [lesson.type, lesson.level, lesson.duration].map(function (item) { return '<span class="meta-tag">' + escapeHtml(item) + "</span>"; }).join("") + "</div>", '<div class="detail-panel-actions">', '<button class="primary-button" type="button" data-detail-action="toggle">' + (completed ? "取消完成" : "标记已学") + "</button>", '<button class="secondary-button" type="button" data-detail-action="next">下一课</button>', "</div>", "</div>", '<div class="detail-body">', '<div class="detail-block"><h4>像什么？</h4><p>' + escapeHtml(lesson.metaphor) + "</p></div>", '<div class="detail-block"><h4>学完你会什么</h4><ul>' + lesson.learned.map(function (item) { return "<li>" + escapeHtml(item) + "</li>"; }).join("") + "</ul></div>", '<div class="detail-block"><h4>学习步骤</h4><ol>' + lesson.steps.map(function (item) { return "<li>" + escapeHtml(item) + "</li>"; }).join("") + "</ol></div>", '<div class="detail-block"><h4>动手练习</h4><p>' + escapeHtml(lesson.exercise) + "</p></div>", '<div class="detail-block"><h4>最小示例</h4><pre class="snippet">' + escapeHtml(lesson.snippet) + "</pre></div>", "</div>"].join(""); }
function renderPrompts() { promptGridEl.innerHTML = promptCards.map(function (card) { return ['<article class="prompt-card">', "<header>", "<div>", "<h3>" + escapeHtml(card.title) + "</h3>", "<p>直接复制给 Codex 就能用。</p>", "</div>", '<button type="button" class="copy-button" data-copy="' + escapeHtmlAttribute(card.body) + '">复制</button>', "</header>", '<p class="prompt-text">' + escapeHtml(card.body) + "</p>", '<ul>' + card.tips.map(function (tip) { return "<li>" + escapeHtml(tip) + "</li>"; }).join("") + "</ul>", "</article>"].join(""); }).join(""); }
function renderDeployment() { deploymentGridEl.innerHTML = deploymentCards.map(function (card) { return ['<article class="deployment-card">', "<h3>" + escapeHtml(card.title) + "</h3>", "<p>" + escapeHtml(card.intro) + "</p>", "<ul>" + card.items.map(function (item) { return "<li>" + escapeHtml(item) + "</li>"; }).join("") + "</ul>", "</article>"].join(""); }).join(""); }
function filteredLessons() { return lessons.filter(function (lesson) { const inType = activeType === "全部" || lesson.type === activeType; const haystack = [lesson.title, lesson.summary, lesson.metaphor, lesson.track, lesson.type, lesson.level, lesson.duration, lesson.tags.join(" ")].join(" ").toLowerCase(); const matchesQuery = !queryText || haystack.indexOf(queryText) !== -1; return inType && matchesQuery; }); }
function toggleLesson(lessonId) { const index = progressStore.completed.indexOf(lessonId); if (index === -1) { progressStore.completed.push(lessonId); showToast("已标记为完成"); } else { progressStore.completed.splice(index, 1); showToast("已取消完成"); } saveProgress(); renderAll(); }
function updateProgress() { const completed = progressStore.completed.length; const percent = Math.round((completed / lessons.length) * 100); completedCountEl.textContent = String(completed); progressPercentEl.textContent = percent + "%"; const conic = Math.max(0, Math.min(360, (completed / lessons.length) * 360)); document.querySelector(".progress-ring").style.background = "radial-gradient(circle at center, rgba(8, 16, 31, 0.96) 55%, transparent 56%), conic-gradient(var(--secondary) 0deg, var(--primary) " + conic + "deg, rgba(255, 255, 255, 0.08) " + conic + "deg 360deg)"; }
function loadProgress() { try { const raw = localStorage.getItem(storageKey); if (!raw) return { completed: [] }; const parsed = JSON.parse(raw); if (!parsed || !Array.isArray(parsed.completed)) return { completed: [] }; return { completed: parsed.completed.filter(function (id) { return lessons.some(function (lesson) { return lesson.id === id; }); }) }; } catch (error) { return { completed: [] }; } }
function saveProgress() { try { localStorage.setItem(storageKey, JSON.stringify(progressStore)); } catch (error) {} }
function showToast(message) { toastEl.textContent = message; toastEl.classList.add("visible"); clearTimeout(showToast.timer); showToast.timer = setTimeout(function () { toastEl.classList.remove("visible"); }, 1800); }
function escapeHtml(text) { return String(text).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;"); }
function escapeHtmlAttribute(text) { return escapeHtml(text).split("\n").join("&#10;"); }